{
  "name": "garax-admin",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "nx next:dev -p 3002",
    "build": "cross-env NODE_ENV=production nx next:build",
    "build:product": "cross-env ANALYZE=true yarn build",
    "start": "nx next:start",
    "lint": "eslint . --ext .ts,.tsx,.js",
    "export": "next export"
  },
  "importSort": {
    ".js, .jsx, .ts, .tsx": {
      "style": "module",
      "parser": "typescript"
    }
  }
}

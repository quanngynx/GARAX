import { CSSProperties } from "react";

export const customDefault: CSSProperties = {
    backgroundColor: '#fff',
    borderRadius: '50%',
    padding: 24,
    zIndex: 100,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    cursor: 'pointer',
    width: 40,
    height: 40,
}
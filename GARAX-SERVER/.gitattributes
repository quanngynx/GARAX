# Set line terminators for cross-platform
# Automatically detect and normalize line endings
* text=auto

# Markup JSON, JS, and other configuration files
*.js text eol=lf
*.json text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.env text eol=lf

# Ignore binary files, no need to handle diff
*.png binary
*.jpg binary
*.gif binary
*.pdf binary
*.zip binary
*.exe binary

# If you use Git LFS (Large File Storage) to store large files
# Uncomment the line below if Git LFS is present
# *.psd filter=lfs diff=lfs merge=lfs -text

# Specify log and data files that do not require version control
*.log -text
*.sql -text
